package com.mergetechng.jobs.A.job.application.project.to.create.view.and.Send.Job.Seekers.Information.to.the.employer.common.enums;

public enum RequestType {
    GET,
    POST,
    DELETE,
    UPDATE,
    PURGE,
    PATCH
}
